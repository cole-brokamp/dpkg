# get github token from GITHUB_PAT environment variable or use bundled token if unset

get github token from GITHUB_PAT environment variable or use bundled
token if unset

## Usage

``` r
get_gh_token()
```
